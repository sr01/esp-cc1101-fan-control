button:
  - platform: template
    name: Left Fan Wind Button
    on_press:
      - script.execute: left_fan_wind

  - platform: template
    name: Left Fan F/R Button
    on_press:
      - script.execute: left_fan_direction

  - platform: template
    name: Left Fan 1H Button
    on_press:
      - script.execute: left_fan_1h

  - platform: template
    name: Left Fan 4H Button
    on_press:
      - script.execute: left_fan_4h

  - platform: template
    name: Left Fan 8H Button
    on_press:
      - script.execute: left_fan_8h
